
` comment "this is a syntax example"

`assign max `function [a b] [
    `if `le a b [
        `return b,
    ][
        `return a
    ]
]

= max ^[a b] [
    ? `'<= a b [
        ~ b
    ][
        ~ a
    ]
]

{
    a b
    c d,
    e f,
    g h,
}

(`while true [])

<for i min, max []>
